W6-A Talk Room Manual Run - PASS Checklist (2 browsers, 1 room)
Date: 2026-02-28
Scope: Week 6-A realtime hardening validation (docs-first)

A4 Decision Confirmation (must be explicit)
- Polling-only chosen for v1.
- WebSockets deferred for Week 6-A.
- Source of truth endpoint: GET /api/chat-night/room/{room_id}

Pre-check setup
1. Start backend:
   cd C:\001_dating_app\blush_hour_v0\backend
   venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
2. Start web app:
   cd C:\001_dating_app\blush_hour_v0\mobile-app
   npm run web
3. Open Browser A and Browser B with separate authenticated users.
4. Put both users in the same Talk Room.
5. Keep browser DevTools Network panel open in at least one browser.

Polling behavior expectations
- Foreground poll cadence: 2000ms
- Background poll cadence: 4000ms
- Error backoff cadence: 5000ms
- Immediate poll triggers: app active, tab visibility return, window focus

PASS scenarios (required unless marked optional)

1. Normal sync (required)
- Keep both tabs active for at least 60 seconds.
- PASS if timer drift remains <= 2 seconds and engage state remains consistent on both clients.

2. Background snap-back (required)
- Background Browser B for 20-30 seconds.
- Bring Browser B to foreground.
- PASS if an immediate poll occurs and timer/state snap back within one poll cycle.

3. Engage unlock sync (required)
- Browser A taps Engage first.
- Verify A shows waiting_for_partner while B remains pending.
- Browser B taps Engage.
- PASS if both clients converge to match_unlocked and navigation/alerts happen once (no duplicates).

4. Backend stop/restart recovery (required, A3 behavior)
- Stop backend for 20-30 seconds while both clients remain on Talk Room.
- PASS if clients do not crash and show recoverable retry/error state.
- Restart backend.
- PASS if polling recovers automatically and room state resyncs without manual page refresh.

5. 429 rate-limit behavior (optional but recommended)
- Trigger throttling using existing test method/hook.
- PASS if client shows throttled/retry feedback and continues with bounded backoff.

Final PASS gate
- Required scenarios 1-4 must pass.
- Scenario 5 result should be logged if executed.
- Any fail must record step number, timestamp, browser, and observed behavior.
